paths-ignore:
  # Firebase client-side config apiKey is intentionally public.
  # Security is enforced via Firestore rules, not key secrecy.
  # See: https://firebase.google.com/docs/projects/api-keys
  - "public/index.html"
  - "js/firebase-config.js"
  - "js/wizelife-auth.js"
  - "src/**"
